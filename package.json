{
    "name": "portfolio-ts",
    "private": true,
    "version": "0.0.0",
    "type": "module",
    "scripts": {
        "dev": "vite",
        "build": "tsc && vite build",
        "preview": "vite preview",
        "predeploy": "npm run build",
        "deploy": "gh-pages -d dist",
        "test": "vitest",
        "test:run": "vitest run"
    },
    "devDependencies": {
        "gh-pages": "^6.3.0",
        "typescript": "~6.0.2",
        "vite": "^8.0.12",
        "vitest": "^4.1.8"
    }
}
